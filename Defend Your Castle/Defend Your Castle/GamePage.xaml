<SwapChainBackgroundPanel
    x:Class="Defend_Your_Castle.GamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Defend_Your_Castle"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loaded="SwapChainBackgroundPanel_Loaded">

    <SwapChainBackgroundPanel.Resources>
        <Style x:Key="PauseMenuStyle" TargetType="Border">
            <Setter Property="Background" Value="LightBlue"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10"/>
        </Style>
        
        <!-- Weapon Button Style -->
        <Style x:Key="WeaponButtonStyle" BasedOn="{StaticResource RoundedButtonStyle}" TargetType="Button">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="80"/>
        </Style>
    </SwapChainBackgroundPanel.Resources>

    <Canvas Name="CurrentScreen" x:FieldModifier="public">
        
    </Canvas>
    
    <Grid Name="GameHUD" x:FieldModifier="public" Margin="20" Visibility="Collapsed">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Name="HUD_TopRow">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Top">
                <Button Name="HUD_WeaponSword" Style="{StaticResource WeaponButtonStyle}" BorderBrush="Black" />
                <Button Name="HUD_WeaponMace" Style="{StaticResource WeaponButtonStyle}" BorderBrush="Black" Margin="30,0,30,0" />
                <Button Name="HUD_WeaponWarhammer" Style="{StaticResource WeaponButtonStyle}" BorderBrush="Black" />
            </StackPanel>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Name="HUD_OuterHPBar" VerticalAlignment="Top" CornerRadius="1" Height="50" Background="Black" BorderBrush="Black" BorderThickness="1">
                    <Border Name="HUD_InnerHPBar" CornerRadius="1" Background="Red" BorderBrush="Transparent" BorderThickness="0" />
                </Border>

                <TextBlock Text="Gold: 500" FontSize="24" Foreground="Black" Grid.Row="1" Margin="0,5,0,0"/>
            </Grid>
        </Grid>
    </Grid>

    <Border Name="PauseMenu" Style="{StaticResource PauseMenuStyle}" x:FieldModifier="public" Visibility="Collapsed">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Content="Menu Option 1" IsTabStop="False"></Button>
            <Button Content="Menu Option 2" IsTabStop="False" Grid.Row="1"></Button>
            <Button Content="Menu Option 2" IsTabStop="False" Grid.Row="2"></Button>
            <Button Content="Menu Option 2" IsTabStop="False" Grid.Row="3"></Button>
        </Grid>
    </Border>

    <Canvas Name="Shop" x:FieldModifier="public" Visibility="Collapsed">

    </Canvas>
</SwapChainBackgroundPanel>